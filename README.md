# Kotlin Multipliplatfom Sample

* Implements Android and iOS trgets
* Uses MVVM Design pattern (LiveData arch component on Android, SwiftUI and Combine on iOS)
* Uses Cocoapods plugin for building iOS targets
